---
# Allowed version bumps: patch, minor, major
default: minor
---

Uploaded images now also get an 800px rendition, named `thumbnail3`, alongside the existing 150px and 350px ones, so consumers needing a larger preview no longer have to fall back to the original file.
